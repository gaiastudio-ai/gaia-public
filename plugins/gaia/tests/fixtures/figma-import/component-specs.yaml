# component-specs.yaml — captured fixture for VCP-UX-06 / E46-S2.
#
# Reference output of /gaia-create-ux Import mode against a small Figma
# file with two component sets (button, card). Used as the regression
# contract for the schema_version + components: shape; not consumed at
# runtime.
schema_version: "1.0"
_meta:
  source: figma
  figma_file_key: "ABC123fixturekey4567890"
  generated_by: "gaia-create-ux import-mode (E46-S2 fixture)"
components:
  button:
    name: "Button"
    figma_node_id: "1:42"
    variants:
      - "primary"
      - "secondary"
      - "tertiary"
    states:
      - "default"
      - "hover"
      - "active"
      - "disabled"
      - "loading"
    props:
      label: "string"
      icon: "string?"
      onPress: "() => void"
    platform_tokens: {}
  card:
    name: "Card"
    figma_node_id: "1:108"
    variants:
      - "elevated"
      - "outlined"
    states:
      - "default"
      - "hover"
    props:
      title: "string"
      body: "string"
    platform_tokens: {}
