# Epics and Stories (flat layout)

This file is a fixture for E53-S233 acceptance tests. Its presence — without a sibling sharded `epics/` directory — proves the `validate-gate.sh epics_and_stories_exists` resolver continues to accept the legacy flat `{dir}/{name}.md` layout (regression guard for AC5).
