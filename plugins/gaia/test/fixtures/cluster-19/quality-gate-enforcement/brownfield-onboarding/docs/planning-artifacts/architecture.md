# Architecture — Cluster 19 Brownfield Quality Gate Enforcement Fixture

Fixture seed for the `brownfield-onboarding` post-complete gate. Both
`nfr-assessment.md` and `performance-test-plan.md` are deliberately absent
from `docs/test-artifacts/` so the gate must HALT `brownfield-onboarding`.
