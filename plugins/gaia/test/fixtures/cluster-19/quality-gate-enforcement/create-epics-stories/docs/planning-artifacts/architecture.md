# Architecture — Cluster 19 Quality Gate Enforcement Fixture

Fixture seed for the `create-epics-stories` gate-under-test variant.
The `docs/test-artifacts/test-plan.md` artifact is deliberately absent so the
`test_plan_exists` gate must HALT `create-epics-stories`.

## Components

- Fixture-only — no code under test.
