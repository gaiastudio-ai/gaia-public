#!/usr/bin/env bash
# minimal-source.sh — fixture for Phase 1 test-automate tests.
# A trivially small source file that Phase 1 should analyze and include
# in analyzed_sources[] with a valid sha256:{hex} entry.
hello() { echo "world"; }
