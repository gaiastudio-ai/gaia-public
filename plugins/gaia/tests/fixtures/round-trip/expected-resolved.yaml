# expected-resolved.yaml — E28-S141 round-trip fixture (expected merged output)
#
# The expected resolution of shared-project-config.yaml overlaid by
# local-global.yaml. project_path carries the LOCAL value (ADR-044 precedence).
# All other keys pass through from the shared file unchanged because the local
# file did not override them.
#
# This file is also the authoritative input for the
# "AC5: expected-resolved fixture passes resolve-config.sh without errors" test
# — resolve-config.sh must accept this shape as-is with zero code changes,
# proving the resolver treats the split generically per ADR-044.

project_root: /srv/ci/gaia-framework
project_path: /Users/alex/Dev/gaia-enterprise
memory_path: /srv/ci/gaia-framework/_memory
checkpoint_path: /srv/ci/gaia-framework/_memory/checkpoints
installed_path: /srv/ci/gaia-framework/_gaia
framework_version: 1.127.2-rc.1
date: 2026-04-17
