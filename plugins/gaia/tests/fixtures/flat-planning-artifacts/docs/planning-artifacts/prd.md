# PRD (flat layout)

This file is a fixture for E53-S233 acceptance tests. Its presence — without a sibling sharded `prd/` directory — proves the `validate-gate.sh prd_exists` resolver continues to accept the legacy flat `{dir}/{name}.md` layout (regression guard for AC4).
